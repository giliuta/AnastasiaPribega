[build]
buildCommand = "cd frontend && yarn install && yarn build && cd .. && pip install -r backend/requirements.txt"

[deploy]
startCommand = "cd backend && python -m uvicorn server:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
